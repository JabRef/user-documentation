# Token Estimators

Some of the algoritms in AI feature reequire knowing if a message would overflow LLM's context window. At the same time the tokenizator that is used with LLM is sometimes not accessible. For this reason JabRef has a separate option called "token estimator" that 
